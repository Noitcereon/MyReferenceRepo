// Make use of this in vuex modules.
// This makes it easy to replace the api url, should it change.
// In addition, by using the same base url for all calls, they have a similar structure (as they have the same starting point)
const baseApiUrl = "https://dk-hvz-backend.herokuapp.com/api/";

export default baseApiUrl;
